Once installed, create a new directory to store your project:

`mkdir rasa`
`cd rasa`

initiate a project:

`rasa init`

Now add some training data pls in the `/root/rasa/data/nlu.yml` file

and retrain 

*commence party*